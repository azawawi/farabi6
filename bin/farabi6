#!/usr/bin/env perl6
use v6;

BEGIN { @*INC.unshift('./lib'); }

use Farabi6;

sub MAIN(Str :$hostname = 'localhost', Int :$port = 3030) {
	say "Farabi6 is opening a webserver at http://$hostname:$port";
	Farabi6.new.run($hostname, $port);
}
