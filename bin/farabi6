#!/usr/bin/env perl6
use v6;

BEGIN { @*INC.unshift('./lib'); }

use Farabi6;

sub MAIN(Int :$port = 3030) {
	say "Farabi is opening a webserver at http://127.0.0.1:$port";
	Farabi6.new.run($port);
}
